* Merge PmwikiPage and PmwikiConfig?
* Verbosity level support
* Fix logger
* tmpfiles: make a directory and use real page name as file name
* move to Python 3
* use subcommands <https://docs.python.org/2/library/argparse.html#sub-commands>
  * this requires cutting the subcommands into separate functions
